# React Native / Expo's Metro bundler and native build tooling assume a flat,
# hoisted node_modules. pnpm's default isolated layout breaks them, so we hoist.
node-linker=hoisted
shamefully-hoist=true
